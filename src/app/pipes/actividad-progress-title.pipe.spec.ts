import { ActividadProgressTitlePipe } from './actividad-progress-title.pipe';

describe('ActividadProgressTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ActividadProgressTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
